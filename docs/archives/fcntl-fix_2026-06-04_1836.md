# fcntl Windows Compatibility + Test Fix — 2026-06-04

## Objective
Fix `fcntl` import error preventing security tests from running on Windows, and fix broken test method name.

## Key Changes
1. **security.py**: Added platform detection — `fcntl` on Linux, `msvcrt` on Windows for file locking in `RateLimiter._atomic()`.
2. **security.py**: Added `_load()` method as alias for `_load_unlocked()` (wrapped in `_atomic()`) — tests were calling `_load()` but method was renamed to `_load_unlocked()` during earlier refactor.

## Results
- **67/67 security tests PASS** ✅
- Coverage check fails (7% < 78%) but that's a project-wide coverage issue, not related to these changes
- Windows and Linux both supported for file-based rate limiting

## Conclusions
All security fixes are now testable and verified on Windows. The fcntl/msvcrt compatibility layer ensures the RateLimiter works cross-platform.
