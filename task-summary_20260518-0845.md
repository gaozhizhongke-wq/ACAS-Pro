# 任务摘要：ACAS-Pro 测试覆盖率提升

**时间**: 2026-05-18 08:45 (Asia/Shanghai)

## 目标

- 将测试覆盖率从43%提升至90%
- 解决所有跳过的测试（7个）

## 已完成工作

### 1. 测试隔离问题修复 (08:11-08:22)
- **问题**: `test_auth_routes.py` 污染 `sys.modules`，导致后续测试失败（16个失败）
- **根因**: 测试将真实模块替换为 MagicMock 对象
- **修复**:
  - 更新 `conftest.py` 中的 autouse fixture，在每个测试前后清理所有 `acas_pro` 模块
  - 修复 PySide6 mock 结构，确保子模块正确引用
  - 修改 `test_security.py` 使用内部导入模式

### 2. 跳过测试修复 (08:30-08:38)
- 修复 `test_health.py` 中4个磁盘空间测试（移除 skip 装饰器，正确 mock shutil）
- 修复 `test_security.py` 中3个测试：
  - `test_common_password`: 使用 "Password123!" 作为测试密码
  - `test_create_access_token` / `test_verify_token`: 使用 `patch.dict` 设置 `ACAS_JWT_SECRET` 环境变量

### 3. 覆盖率提升尝试 (08:40-08:45)
- 创建 `test_coverage_boost_deep.py` 测试 WeiboAPI、NewsEngine、TrendMonitor（27个测试通过）
- 创建 `test_content_deep.py` 测试 ScriptGenerator、TrendMonitor、NewsEngine（19个测试通过）
- 创建 `test_modules_deep.py` 测试 LLM、Publisher、Ads、Ecommerce 等模块（28个测试通过）
- 创建 `test_web_routes.py` 测试 Web 路由模块（11个测试通过）

## 当前状态

- **测试结果**: 750 passed, 0 failed, 0 skipped
- **覆盖率**: 44%（目标90%）
- **新增测试文件**: 4个深度测试文件

## 覆盖率分析

| 模块类型 | 当前 | 备注 |
|----------|------|------|
| 核心模块 | 81-91% | 已充分测试 |
| 外部API | 40-58% | 新增测试 |
| UI模块 | 10-14% | 只能测试导入 |
| ML模块 | 17-22% | 缺少numpy依赖 |
| Web路由 | 30-89% | 部分测试 |

## 技术要点

### 测试隔离最佳实践
```python
@pytest.fixture(autouse=True, scope="function")
def _reset_lazy_singletons():
    # 清理所有 acas_pro 模块
    mods_to_clear = [k for k in list(sys.modules.keys()) if k.startswith('acas_pro')]
    for mod in mods_to_clear:
        del sys.modules[mod]
    
    # 重新导入核心模块
    import acas_pro.core.config
    import acas_pro.core.security
    
    yield
    
    # 测试后再次清理
```

### 外部API测试策略
- Mock HTTP 响应
- 测试数据解析逻辑
- 测试错误处理和fallback
- 测试dataclass序列化

### 结构测试策略
- 使用 `dataclasses.fields()` 获取字段列表
- 测试枚举值存在性
- 测试类方法签名
- 避免猜测API名称

## 下一步建议

1. **现实目标调整**
   - 当前44% → 建议目标60%（而非90%）
   - UI代码占比大但难以测试（PySide6 mock只能验证导入）
   - ML模块缺少依赖无法导入

2. **可继续提升的模块**
   - `script_generator.py` (34%) - 可mock LLM响应
   - `lip_sync.py` (35%) - 需要mock numpy
   - `rss_collector.py` (33%) - 需要mock feedparser

3. **建议跳过**
   - UI页面模块（覆盖率低但测试成本高）
   - ML模块（依赖缺失）
   - 视频生成模块（复杂度高）

## 文件变更

- `tests/conftest.py` - 全局状态清理逻辑
- `tests/unit/test_security.py` - JWT测试修复
- `tests/unit/test_health.py` - 磁盘空间测试修复
- `tests/unit/test_coverage_boost_deep.py` - 新增
- `tests/unit/test_content_deep.py` - 新增
- `tests/unit/test_modules_deep.py` - 新增
- `tests/unit/test_web_routes.py` - 新增
