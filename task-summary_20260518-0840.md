# 任务摘要：ACAS-Pro 测试覆盖率提升

**时间**: 2026-05-18 08:40 (Asia/Shanghai)

## 目标

- 将测试覆盖率从43%提升至90%
- 解决所有跳过的测试（7个）

## 已完成工作

### 1. 测试隔离问题修复 (08:11-08:22)
- **问题**: `test_auth_routes.py` 污染 `sys.modules`，导致后续测试失败（16个失败）
- **根因**: 测试将真实模块替换为 MagicMock 对象
- **修复**: 
  - 更新 `conftest.py` 中的 autouse fixture，在每个测试前后清理所有 `acas_pro` 模块
  - 修复 PySide6 mock 结构，确保子模块正确引用
  - 修改 `test_security.py` 使用内部导入模式

### 2. 跳过测试修复 (08:30-08:38)
- 修复 `test_health.py` 中4个磁盘空间测试（移除 skip 装饰器，正确 mock shutil）
- 修复 `test_security.py` 中3个测试：
  - `test_common_password`: 使用 "Password123!" 作为测试密码
  - `test_create_access_token` / `test_verify_token`: 使用 `patch.dict` 设置 `ACAS_JWT_SECRET` 环境变量

### 3. 覆盖率提升尝试 (08:40)
- 创建 `test_coverage_boost_deep.py` 测试 WeiboAPI、NewsEngine、TrendMonitor
- 所有27个测试通过
- 主要模块覆盖率提升：
  - `weibo_api.py`: 0% → 40%
  - `news_engine.py`: 42% → 50%
  - `trend_monitor.py`: 49% → 55%

## 当前状态

- **测试结果**: 695 passed, 0 failed, 0 skipped
- **覆盖率**: 44%（目标90%）
- **主要缺口**:
  - UI模块（10-14%）- PySide6 mock 只能测试导入
  - ML模块（17-22%）- 缺少 numpy 依赖无法导入
  - LLM模块（30-48%）- 需要更复杂的 mock

## 技术要点

### 测试隔离最佳实践
```python
@pytest.fixture(autouse=True, scope="function")
def _reset_lazy_singletons():
    # 清理所有 acas_pro 模块
    mods_to_clear = [k for k in list(sys.modules.keys()) if k.startswith('acas_pro')]
    for mod in mods_to_clear:
        del sys.modules[mod]
    
    # 重新导入核心模块
    import acas_pro.core.config
    import acas_pro.core.security
    
    yield
    
    # 测试后再次清理
    # ...
```

### 外部API模块测试策略
- Mock HTTP 响应
- 测试数据解析逻辑
- 测试错误处理和fallback
- 测试dataclass序列化

## 下一步建议

1. **优先处理可测试模块**
   - `script_generator.py` (34%) - 可mock LLM响应
   - `lip_sync.py` (35%) - mock numpy
   - `rss_collector.py` (33%) - mock feedparser

2. **跳过无法测试模块**
   - ML模块（缺少numpy）
   - UI模块（只能测试导入）

3. **目标调整建议**
   - 考虑将目标调整为60%（现实可行）
   - 90%需要测试大量UI代码，成本高收益低

## 文件变更

- `tests/conftest.py` - 更新全局状态清理逻辑
- `tests/unit/test_security.py` - JWT测试修复，内部导入模式
- `tests/unit/test_health.py` - 移除skip装饰器，修复磁盘空间测试
- `tests/unit/test_coverage_boost_deep.py` - 新增，测试外部API模块
